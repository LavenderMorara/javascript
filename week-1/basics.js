//SET UP
// 1. Running js code 
//syntax : node file_name.js
//eg . node conditionals.js

//BASICS
//1. Data types - tells C++ what kind of data a variable can store, and what operations are allowed.
    // 1. number - numbers; both whole and decimal eg let age = 4;  ,  const pi = 3.1459;
    // 2. array - list conatining elements enclosed in square brackets
    //    eg. let fruits = ["apple", "mango", "banana"]
    // 3. undefined - variable lacks  a value but may be later assigned eg. let y;
    // 4. Null - intentionally valueless variables
    // 5. string = text; enclosed in double quotes, single quotes, back ticks ; `` eg. string name = "Chiya";
    // 6. booleans = boooleans ie. true/false eg bool isStudent = true;  
    //falsey values 0, Null, Nan(not a number), undefined, empty string
    //objects - key value pairs enclode in curly braces{}
    //eg. let person = {
    //      name : "Chiya",
    //      age :21,
    //      city : "Nairobi"
    //   };

//2. Variable declarations
//Variables are declared using let or const
//1. let - used while declaring variables that are anticipated to change at some point or when intended to declare 
// but not initialise(attach a value) the variable
//syntax 
// 1. let variable_name_1 = value ;
//    eg. let name = "chiya";
// 2. let variable_name_2 ;
//    variable_name_2 = value ; 
//    eg. let x; 
//     x=4;
//2. const - used when declaring a variables whose values we don'd anticipate to change; must be initialsied upon declaration
//syntax
// const variable_name = value;
//   eg. const town = "Nairobi";

//3. Operators 
// a) arithmetic                      b)assignment operators
// +   -addition                        = - assigns values to variables
// -   -subtraction                     += - x += 5 ie x=x+5
// *   -multiplication                  -= - x -= 5 ie x=x-5
// /   -division                        *= - x *= 5 ie x=x*5
// %   - remainder (modulus)            /= - x /= 5 ie x=x/5

// c)comparison operators                  d)Logical operators           e)Unirary operators
// === - equal to (strict)                && - AND                       ++  - increment
// !== - not equal to  (strict)           || - OR                        --  - decrement
// > - greater than                       !  - NOT                       typeof - find the data type of data
// < - less than 
// >= - greater than or equal to
// <= - less than or equal to




